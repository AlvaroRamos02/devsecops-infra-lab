version: '3'
services:
  dashboard:
    image: nginx:alpine
    container_name: security-dashboard
    ports:
      - "7888:80"
    volumes:
      - ./dashboard:/usr/share/nginx/html:ro
    restart: always
